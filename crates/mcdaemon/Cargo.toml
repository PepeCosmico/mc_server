[package]
name = "mcdaemon"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# IMPORTANTE: Así vinculamos tu librería local
mcprocess = { path = "../mcprocess" }